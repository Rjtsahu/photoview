# Multi-stage build for Photoview with interactive Zoom UI
FROM node:18-alpine AS ui-builder

WORKDIR /app/ui
COPY ui/package*.json ./
RUN npm ci --no-audit --no-fund
COPY ui/ ./
RUN npm run build

FROM photoview/photoview:2
COPY --from=ui-builder /app/ui/dist /app/ui
